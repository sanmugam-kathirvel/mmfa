<%= form_for(@doctor_spel) do |f| %>
  <% if @doctor_spel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor_spel.errors.count, "error") %> prohibited this doctor_spel from being saved:</h2>

      <ul>
      <% @doctor_spel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
